printfn "Type in the pattern you want to repeat."
let InputPattern = System.Console.ReadLine
printfn "Type in the value you want to add to the pattern."
let InputValue = System.Console.ReadLine
printf "Do you want multiple values? "
let Question = System.Console.ReadKey
if Question.Key = System.ConsoleKey.Y then
    let bruh = null



//let RegularExpressionEvaluator = new System.Text.RegularExpressions.Regex()