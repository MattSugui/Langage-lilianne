ask; // ask for a string
push "hello!";
bne 6;
take; // ask but the value has the potential to change to an integral value if it can
push 21;
beq 10;
push "bruh moment";
print;
pop;
end;
push "yay!";
print;
pop;